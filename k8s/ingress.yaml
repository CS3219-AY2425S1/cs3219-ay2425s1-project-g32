apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: peerprep-g32-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: peerprep-fe
            port:
              number: 80